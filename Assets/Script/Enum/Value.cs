namespace Enum
{
    public static class Value
    {

        public enum SquareMaterial
        {
            Ground,
            Red,
            Green,
            Wall
        } 
        
        public enum GameResult
        {
            Playing,
            Win,
            Lose
        }
    }
}